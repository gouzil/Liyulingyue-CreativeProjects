[project]
name = "openai-json-wrapper"
version = "0.1.0"
description = "小型 JSON 封装，适配 OpenAI 风格的数据与自定义类型序列化"
authors = [ { name = "Your Name" } ]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=42","wheel"]
build-backend = "setuptools.build_meta"
